name: Update latest tag

on:
  release:
    types: [prerelease]

jobs:
  update-tag:
    steps:
      - name: Run latest-tag
        uses: EndBug/latest-tag@latest
        with:
          # You can change the name of the tag or branch with this input.
          # Default: 'latest'
          ref: latest-prerelease
